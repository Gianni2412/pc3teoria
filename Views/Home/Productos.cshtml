@model List<Producto>

<h1>lista de Productos</h1>

<table class="table border">
    <tr>
        <th>ID</th>
        <th>NOMBRE</th>
        <th>PRECIO</th>
        <th>FOTO</th>
        <th>CATEGORIA</th>


    </tr>
    @foreach (var p in Model)
    {
        <tr>
            <td>@p.id</td>
            <td>@p.Nombre</td>
            <td>@p.Precio</td>
            <td>@p.Foto</td>
            <td>
                @p.Categoria.Nombre


            </td>




        </tr>
    }

    @if (Model.Count == 0)
    {
        <tr>
            <td colspan="4" class="text-center">No se ha encontrado Productos</td>
        </tr>
    }


</table>

<a href="/Home/NuevoProducto" class="btn btn-dark">Añadir Producto</a>